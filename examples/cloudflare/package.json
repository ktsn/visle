{
  "name": "cloudflare-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp build",
    "check:worker": "vp build && wrangler deploy --dry-run",
    "deploy:worker": "vp build && wrangler deploy",
    "dev": "vp dev",
    "preview": "vp preview"
  },
  "dependencies": {
    "hono": "^4.11.9",
    "visle": "workspace:*",
    "vue": "^3.5.28"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.50.0",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "^4.118.0"
  }
}
